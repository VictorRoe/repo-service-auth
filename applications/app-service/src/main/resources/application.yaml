server:
  port: 8083
spring:
  application:
    name: "Authentication"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080,http://localhost:8083,http://localhost:8087"

adapters:
  r2dbc:
    host: ${DB_HOST:localhost}
    port: ${DB_PORT:5433}
    database: ${DB_NAME:auth_db}
    schema: ${DB_SCHEMA:public}
    username: ${DB_USERNAME:admin}
    password: ${DB_PASSWORD:12345}

jwt:
  secret: ${JWT_SECRET}
  expiration: 3600

logging:
  level:
    org.springframework.security: DEBUG